import { defineComponents } from 'blume'

export default defineComponents({
  layout: {
    // Mermaid click-to-expand lightbox (see components/MermaidZoom.astro).
    PageFooter: './components/MermaidZoom.astro',
  },
})
