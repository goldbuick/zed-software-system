/** Marquee tape for scroll panel key help (task flow: act → move → close → jumps). */
export const SCROLL_KEY_HINTS_LINE = `
keys: $whiteenter$green.ACTION ON SELECTED LINE 
$whiteup/down$green.SCROLL UP/DOWN 
$whiteesc/cancel$green.CLOSE SCROLL 
$whitealt+up/down$green.JUMP 10 LINES 
$white$meta+up/down$green.JUMP TOP/BOTTOM $blue
`
