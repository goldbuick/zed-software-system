# validate.ts

**Purpose**: Small format validators with no domain dependencies.

## Exports

| Export | Description |
|--------|-------------|
| `isemail(email)` | Returns a truthy match object if `email` matches a common email pattern; otherwise null |
