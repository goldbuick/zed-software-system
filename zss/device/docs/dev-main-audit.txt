DEV VS MAIN DELTA AUDIT (implementation of plan; do not edit plan file)
Repo: zed-software-system. Range: main..dev (56 commits at audit time).

=== 1. BASELINE LOG — commit themes (main..dev) ===
Themes by message keywords (many merges/version bumps interleaved):

  boardrunner   — board runner work, working on board runners, user input (pilot)
  jsonsync      — reworking jsonsync, jsonsync, bef420065
  forward/net   — selective forwarding for netterminal, selective forward, 72b7d328f
  perf          — perf work (80d4e3d3a)
  ci/version    — ci: version bump to 1.2.*
  merge/sync    — Merge branch dev, sync work, ".", sync work wow/o-o/UGH

Rough counts from one-line log: ~15 ci bumps, ~12 sync/merge/dot, ~6 boardrunner,
~3 jsonsync/forward, 1 perf, remainder misc.

=== 2. VERTICAL SLICES (host-vs-join-architecture.md §3.1 / §4.1) ===

HOST (§3.1 order):
  1) sim clock -> ticktock -> vm handletick
  2) memorytickmain(halt, loadersOnly=true) — loaders; any-board access stays here
  3) memorysyncpushdirty -> jsonsyncserver updates / patches toward clients
  4) memoryscanplayers(tracking); boardrunner election; registerboardrunnerask;
      memorysyncrevokeboardrunner on churn; boardrunnerowned refresh
  5) sim forward -> main hub: ticktock, patches, boardrunner:ownedboards, ...
  6) main -> boardrunner worker (allowticktock): ticktock, jsonsync, user, ...
  7) boardrunner: jsonsync:changed -> memoryhydratefromjsonsync; ticktock ->
      pilottick -> memorytickmain(full) -> boardrunnergadgetsynctick ->
      memoryworkerpushdirty -> jsonsyncclientedit upstream

JOIN (§4.1):
  - stubspace stub vm (operator -> ackoperator); no sim clock/jsonsyncserver
  - ticktock on join main hub (local), forward allowticktock into boardrunnerspace
  - State from host: vm:* acks, jsonsyncclient:*, gadgetclient paint/patch, etc.
  - Peer join bridge uses shouldforwardclienttoserver + shouldnotforwardonpeerclient

=== 3. SHRINK / CONSTRAINTS (recorded) ===
  - Stability-first: prefer logging gates, doc alignment, tests before removing
    dual-path replication.
  - Loaders stay in sim vm (memorytickmain loaders path); loaders may touch any board.
  - Fork A (later) = single authority for elected-board / non-loader work only —
    not collapsing loader tick into boardrunner.
  - Host + /join/ must remain correct.
  - Defer forks B–D (gadget strategy, worker graph, jsonsync surface) until A scoped.

=== 4. NOISE / DEV TOOLING ===
  - boardrunnerspace.ts + boardrunner.ts: console.info gated with import.meta.env.DEV
  - zss/perf/hud.tsx: gated by PERF_UI (zss/config ZSS_DEBUG_PERF_UI); non-prod by default
  - zss/memory/debugactivelistinvariant.ts: opt-in localStorage zssDebugActivelistInvariant

=== 5. ALLOWLIST AUDIT — forward.ts vs host-vs-join §6 + netterminal.ts ===

shouldforwardclienttoserver (forward.ts ~138+):
  Doc: vm, user, modem, jsonsyncserver, gadgetclient, paths sync, desync, joinack,
       needsnapshot, ackboardrunner + literal fast paths.
  Code also: vm:* prefix, modem:* prefix, boardrunner target false (early),
       jsonsyncserver targets, gadgetclient paint/patch literals.

shouldforwardservertoclient (forward.ts ~91+):
  Doc: log, chat, ready, toast, second, ticktock, routed vm/user/heavy/synth/modem/
       bridge/register/gadgetclient/jsonsyncclient/boardrunner, vm:* ack paths.
  Code matches; route.path cases: sync, heavy, joinack, acklook, acklogin,
       ackoperator, ackzsswords, gadgetclient.

shouldnotforwardonpeerserver (peerblockedleaf servertoclient):
  CODE + forward.peer.test.ts: blocks ready, netterminal:cap — does NOT block ticktock
  or second (both explicitly expected false in tests).
  DOC was wrong saying ticktock blocked host->join; doc + mermaid updated to match code.

shouldnotforwardonpeerclient: blocks ticktock, second, ready, netterminal:cap
  (+ nested modem:second per test). Matches doc join->host blocked list.

shouldforwardclienttoboardrunner: ticktock, second, ready, user, jsonsyncclient,
  boardrunner — matches doc.

shouldforwardboardrunnertoclient: jsonsync:changed false; ticktock/second leaves
  false at worker egress; jsonsyncserver passthrough; else shouldforwardservertoclient.

netterminal.ts: hostbridge filters shouldforwardservertoclient &&
  !shouldnotforwardonpeerserver; joinbridge filters shouldforwardclienttoserver &&
  !shouldnotforwardonpeerclient; join createforward { allowticktock: true }.

=== 6. DELIVERABLE — authority / wire (short) ===

Authority (conceptual):
  - Canonical MEMORY + jsonsyncserver: host sim worker
  - Loader pass + election: host sim vm tick
  - Elected board tick + pilot + gadget push: boardrunner worker
  - Join: mirror via jsonsyncclient + forwarded gadget; stub vm for bootstrap only

Wire (PeerJS): see corrected §5 in host-vs-join-architecture.md; tests in
  zss/device/__tests__/forward.peer.test.ts are the peer-gate contract.
