@player

' create content
#book temp create

' load content in edit frame
#frame temp edit title

' setup ui
#gadget right 20
 
 $white   - - - - -
 $black$ongrey    ZED.CAFE    
 $white    - - - - -
 
!hotkey plot space " Space ";Plot
!hotkey draw tab "  Tab  ";Draw

' main loop
#cycle 1
#set blinkrate
#clear drawing

:main
#give blinkrate

#if blinkrate < 6 do
  #char 219
#else if blinkrate < 7 do
  #char 176
#else do
  #clear blinkrate
#endif

#if inputmove do
  #if inputalt do
    ?10 inputmove
  #else do 
    ?inputmove
  #endif
#else do
  #idle
#endif

#main

:plot
#put edit idle wall
#main

:draw
#if drawing do
  #clear drawing
  #color white
#else do
  #set drawing
  #color yellow
#endif
#main

' #put temp 
' #copy edit idle paste1
' #put edit idle paste1
' how can we show the copy & paste buffers ?
' and we want copy & paste to be across boards right ?
' so we can have an edit frame turned on for a board
' local memory, but also need a shared edit memory across boards
' we dont have a generic way of referring to types
' we can just guess honestly ?
' so do we need to be able to "peek" at something?
' peek at a buffer or kind, or color
' we should be able to use buffer contents to do queries against edit
'

