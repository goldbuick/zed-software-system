/// <reference types="vite/client" />

// eslint-disable-next-line @typescript-eslint/consistent-type-definitions -- Vite env augmentation uses interface merging
interface ImportMetaEnv {
  readonly ZSS_PERF_UI: string
}
