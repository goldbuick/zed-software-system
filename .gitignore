# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
tmp
.tmp
dist
headless/dist
dist-bin
dist-server
oclif.manifest.json
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# content build output
ops/fixtures/content/dist

# runtime data
.zss-data

# vosk speech recognition models (large binaries)
cafe/public/models/

# wanix smoke wasm scratch (regenerate with yarn task run wanix:wasm:build)
ops/fixtures/wanix/*.wasm

# offline song renders (regenerate with yarn level-issue-song:render)
cafe/public/renders/*.wav
cafe/public/renders/*.json
cafe/public/renders/*.txt
cafe/public/renders/env-parity/
cafe/public/renders/synth-env-parity/

# local Emscripten SDK (yarn daisy:build)
.emsdk/

# Jest coverage (yarn task run app:test:coverage)
ops/coverage/

# Playwright
test-results/
playwright-report/

# stray wrangler cache at repo root (deploy configs live in ops/infra/)
/infra/

# ZNS generated previews (yarn task run zns:email:preview / zns:grid:preview)
ops/infra/generated/


