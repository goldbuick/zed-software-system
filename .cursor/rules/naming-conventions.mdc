---
description: Naming conventions for methods, variables, and constants
globs: **/*.ts,**/*.tsx
alwaysApply: true
---

# Naming conventions

Apply these names consistently. Do not use camelCase for methods or non-const variables.

## Methods and functions (non-React)

- **Format:** `lowercaseoneword` (all lowercase, no separators).
- Use for: function names, method names, exported helpers.

```typescript
// ✅ GOOD
function getuser(id: string) {}
function memoryreadoperator() {}
export function applypermissionconfig(name: string) {}

// ❌ BAD
function getUser(id: string) {}
function memoryReadOperator() {}
```

## Non-const variables

- **Format:** `lowercaseoneword`.
- Use for: `let`, `var`, parameters, and any variable that is not a constant.

```typescript
// ✅ GOOD
let playertoken = ''
const result = fetchdata()
function run(name: string, count: number) {}

// ❌ BAD
let playerToken = ''
function run(userName: string, itemCount: number) {}
```

## Constants

- **Format:** `UPPER_SNAKE_CASE` (e.g. `TOTAL_ROWS`, `MEMORY_LABEL`).
- Use for: `const` values that are configuration, enums, or fixed values.

```typescript
// ✅ GOOD
const TOTAL_ROWS = 25
const MEMORY_LABEL = { MAIN: 'main' }
const DEFAULT_TIMEOUT_MS = 5000
```

## React components

- **Unchanged:** React component functions stay PascalCase (e.g. `function TapeEditor() {}`).
- Apply lowercaseoneword only to non-component functions and variables inside components.

```typescript
// ✅ GOOD
export function TerminalInput() {
  const [value, setvalue] = useState('')
  const handlechange = () => {}
  return <input onChange={handlechange} />
}
```

## Device message targets

Device message targets use `device:action` (optional further `:segments`). Each **segment** is `lowercaseoneword` — same as function naming.

- **No `-` (kebab)** in `device.emit(...)` target strings or handler registry keys.
- Prefer matching the `api.ts` helper name to the action segment (`vmloader` → `vm:loader`).
- Emit in `zss/device/api.ts` and the owning device’s `handlers/registry.ts` key must stay in lockstep.

```typescript
// ✅ GOOD
device.emit(player, 'vm:loader')
device.emit(player, 'register:terminalopen')
// registry: { loader: handle…, terminalopen: handle… }

// ❌ BAD
device.emit(player, 'vm:load-file')
device.emit(player, 'register:terminal-open')
// registry: { 'load-file': …, 'terminal-open': … }
```

## Summary

| Kind                    | Format              | Example            |
|-------------------------|---------------------|--------------------|
| Methods / functions     | lowercaseoneword    | `memoryreadflags`   |
| Non-const variables     | lowercaseoneword    | `playertoken`       |
| Constants (`const`)     | UPPER_SNAKE_CASE    | `TOTAL_ROWS`       |
| React component names   | PascalCase          | `TerminalInput`    |
| Device message targets  | `device:lowercase`  | `vm:loader` |
