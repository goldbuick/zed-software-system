{
  "name": "zss-youtube-relay",
  "productName": "Zed Cafe YouTube Relay",
  "version": "1.11.60",
  "private": true,
  "description": "Local WHIP to YouTube RTMPS relay for zed.cafe (Tauri v2)",
  "author": "zed.cafe",
  "license": "MIT",
  "scripts": {
    "start": "tauri dev",
    "fetch-binaries": "node scripts/fetch-binaries.mjs",
    "dist": "node scripts/stage-tauri-resources.mjs && tauri build",
    "dist:mac": "node scripts/stage-tauri-resources.mjs && tauri build",
    "dist:win": "node scripts/stage-tauri-resources.mjs && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
