# Media queue Tauri (Rust)

Scaffold for **Zed Cafe Media Queue** (`cafe.zed.media-queue`).

```bash
cd ops/media-queue
yarn install
yarn start
```

Requires [Tauri v2 prerequisites](https://v2.tauri.app/start/prerequisites/). Prefer `yarn task run mediaqueue:dev` from the repo root.
