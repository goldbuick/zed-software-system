# Dev-served static fixtures

Use for ops-owned assets that need browser URLs without living in `cafe/public/`.

Served at `/fixtures/` in dev only (see `vite.config.ts` `servefixturesdev`).

Wanix drag-drop `.wasm`/`.tgz` and `zedcafe.wasm` staging live under `wanix/`.

See also: `/renders/` (offline audio).
