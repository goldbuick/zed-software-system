# Post-change audit — June 2026

Baseline and findings after WASM lang, boardrunner sync, and gadget projection churn.

## Phase 0 — baseline

| Command | Result |
|---------|--------|
| `yarn app:lint` | eslint clean; `tsc --noEmit` has pre-existing lang/WASM type issues in `hostcall.ts`, `wasmloader.ts`, `generator.ts` (not introduced by audit) |
| `yarn app:test` | ~157 suites; 120s per-test timeout; exits cleanly (modem global teardown; no `--forceExit`) |
| `yarn app:sloc` | 796 files, 116604 physical lines |
| `yarn docs:check-links` | `yarn task run docs:check-links` (in PR CI) |
| `yarn lang-regression:test` | In PR CI (`docs-and-lang` job) |
| `yarn e2e:test` | PR CI runs smoke + gadget + join via env flags |
| `yarn app:audit:deadcode` | knip report; 7 unused-file hints (multi-page `cafe/sys`, synth backends — false positives without extra entrypoints) |
| `yarn app:audit:export-catalogs` | Report-only; device/gadget sync exports reconciled in catalogs |

## Audit 1 — dead code

- Tooling: [`knip.json`](../../knip.json), `yarn app:audit:deadcode` (`--no-exit-code`)
- Entrypoints: `cafe/index.tsx`, `cafe/sys/index.tsx`, worker spaces, `headless/src/commands/run.ts`
- Ignore: `zss/feature/synth/archive/**`, `zss/rom/**`, tests
- Export catalog diff: `yarn app:audit:export-catalogs`
- Safe delete: no confirmed orphan files removed (knip unused files are reachable from secondary Vite entries or parity scripts)
- Handler registry: all `vm/handlers/registry.ts` targets documented in [`devices-and-messaging.md`](../../zss/device/docs/devices-and-messaging.md)

## Audit 2 — documentation

Updated: [`zss/ARCHITECTURE.md`](../../zss/ARCHITECTURE.md), [`docs/wasm-sim-port.md`](wasm-sim-port.md), [`docs/multiplayer-wasm-architecture.md`](multiplayer-wasm-architecture.md), [`docs/scroll-validation.md`](scroll-validation.md), [`README.md`](../../README.md), device/gadget `EXPORTED_FUNCTIONS.md` sync sections.

## Audit 3 — performance baselines

Manual: `ZSS_DEBUG_PERF_UI=true` + `!perf` overlay for tick-stage User Timing (`vm:gadgetsynctick`, `vm:boardrunner*`, `vm:gadgetsync:emitdiff`).

Automated (2026-06-06):

| Benchmark | Result |
|-----------|--------|
| `yarn lang:bench:compile` | WASM load 9.6ms; simple_chat_player WASM 4.9ms vs TS 39.3ms (~7.9x); player 4.4ms vs TS 41.0ms (~9.3x) |
| `yarn daisy:bench:synth` | Run locally after `yarn daisy:build`; target realtime factor ≥ 1.0 |
| `yarn app:analyze` | Run locally for WASM chunk sizes under `cafe/public/wasm/` |

Hotspots (ordered): jsonpipe `emitdiff` on memory/boundaries/gadget; `gadgetclient` patch + Zustand; `iso.tsx` rAF; `memorytickmain`; tile texture uploads.

A/B flags: `ZSS_PERF_SPATIAL_INDEX` (ON), `ZSS_PERF_INCREMENTAL_LAYERS` (OFF), `ZSS_PERF_TILE_SUBIMAGE` (OFF).

## Audit 4 — big findings

| ID | Sev | Finding | Action |
|----|-----|---------|--------|
| F-01 | P0 | Gadget sync path had no direct unit tests | Added `gadgetsynctick.test.ts`, `gadgetclient.test.ts` |
| F-02 | P0 | Boardrunner jsonpipe fan-out every tick | Tests added; monitor perf overlay; jsonpipe README notes shadow diff cost |
| F-03 | P0 | `linkdead` had no unit coverage | `linkdead.test.ts` |
| F-04 | P1 | CI e2e was smoke-only | PR CI runs gadget + join specs |
| F-05 | P1 | Lang WASM parity not gated in CI | `lang-regression:test` on PR |
| F-06 | P1 | ARCHITECTURE implied all WASM is future | Docs distinguish lang WASM (shipped) vs sim WASM (planned) |
| F-07 | P1 | Zip import triggers delayed logout | Intentional; manual checklist in scroll-validation |
| F-08 | P2 | Legacy barrels | Do not extend; knip entrypoints use defining modules where possible |
| F-09 | P2 | `PERF_INCREMENTAL_LAYERS` default OFF | A/B when profiling |
| F-10 | P2 | Pre-existing tsc errors in lang WASM | Track separately; does not block eslint/depcruise |

## Audit 5 — regression protection

### Unit tests added

| Module | Test file |
|--------|-----------|
| `gadgetsynctick.ts` | `zss/device/vm/__tests__/gadgetsynctick.test.ts` |
| `boardrunnermemorysync.ts` | `zss/device/vm/__tests__/boardrunnermemorysync.test.ts` |
| `boardrunnerboundarysync.ts` | `zss/device/vm/__tests__/boardrunnerboundarysync.test.ts` |
| `handlers/scroll.ts` | `zss/device/vm/handlers/__tests__/scroll.handlers.test.ts` |
| `handlers/ticktock.ts` | `zss/device/vm/handlers/__tests__/ticktock.orchestration.test.ts` |
| `boardrunner/handlers/linkdead.ts` | `zss/device/boardrunner/handlers/__tests__/linkdead.test.ts` |
| `boardrunner/handlers/tick.ts` | `zss/device/boardrunner/handlers/__tests__/tick.test.ts` |
| `gadgetclient.ts` | `zss/device/__tests__/gadgetclient.test.ts` |

### CI ([`.github/workflows/on-pr-check.yml`](../../.github/workflows/on-pr-check.yml))

- `app:lint`, `app:test`, `native:lint` (matrix)
- `docs:check-links`, `lang-regression:test`
- e2e: `PLAYWRIGHT_INCLUDE_GADGET_E2E=1 PLAYWRIGHT_INCLUDE_JOIN_E2E=1 yarn e2e:test` (30min timeout)

### Coverage

- Thresholds in [`ops/jest.config.ts`](../jest.config.ts) for sync modules
- `yarn app:test:coverage` for targeted collection
